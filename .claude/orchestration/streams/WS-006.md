# WS-006 — Openly Useful project-card link

## Objective

Merge the focused Openly Useful project-card link to the Citewire canonical
website only after that destination is independently verified live over HTTPS.

## Status

**Code complete / held at external destination gate.** The draft is safe to
review but unsafe to merge while citewire.org serves parked HTTP content and
fails HTTPS.

## Provenance

- `Openly-Useful/openlyuseful.org` branch `agent/citewire-site-link`
- SHA `28af5c7a5fdd0425ced2fe45f5c50e05946758de`
- Base main `cf137f9b77d68d16c41cd90f1af8d2df3b9972f4`
- Draft PR #8: `https://github.com/Openly-Useful/openlyuseful.org/pull/8`
- Historical successful validation runs `31849408964` and `31849412743`, plus
  a passing Vercel preview

Trust: high historical evidence; fresh remote checks are required before merge.

## Last-good state

The PR is a two-file focused change, `index.html` and
`scripts/validate_site.py`, and preserves the intervening approved media-kit
work while adding Citewire assertions. No public link has been switched by this
stream.

## Completed

- The card has an accurate Citewire MCP description and targets
  `https://citewire.org` in the draft.
- The validator checks the new link and social-card relationship.
- Green CI/preview evidence was recorded without production deployment.

## Remaining

- Wait for WS-005 to prove a correct public HTTPS destination.
- Freshly verify PR #8 diff, base, mergeability, CI, and preview.
- Review the visible card copy under the current Openly Useful design system.
- Merge only after both destination proof and the applicable owner approval.

## Contradictions and unknowns

- A green preview is not a green external destination.
- The draft's intended citewire.org URL is correct only after WS-005's live
  HTTPS evidence; otherwise the existing production state is safer.
- Current Openly Useful repository permissions, main head, and PR checks are
  Unknown until read again.

## Dependencies

- WS-005 live HTTPS destination gate.
- WS-001 orchestration control.
- Fresh remote PR/CI evidence and applicable owner approval.

## Owned paths

- `openlyuseful.org/index.html`
- `openlyuseful.org/scripts/validate_site.py`

No Citewire, Karaya, DNS, provider, or deployment configuration is owned here.

## Binary definition of done

- citewire.org independently serves the intended HTTPS landing.
- PR #8 remains exactly the expected two-file change on a freshly verified
  current main.
- Openly Useful validator, diff check, CI, and deployment preview pass.
- Merge evidence and post-merge public link verification are recorded.

## Artifact

Fresh PR #8 review, validator output, and post-merge URL verification evidence
linked from KAR-66.

## Checks

- **Baseline:** clean worktree, main/base ancestry, validator, and diff check.
- **Focused:** committed `scripts/validate_site.py` assertions for the Citewire
  card, link, and retained social-card validation.
- **Integration:** Terra independently confirms the card claim and destination
  fit the public-site state; Luna verifies exact target URLs; Sol requires
  WS-005 evidence before authorizing merge sequencing.

## Rollback

Before merge, close or retain the draft without changing production. After an
approved merge, restore the prior card target in a focused, reviewed revert if
the destination later fails; do not use broad repository reset operations.

## Mappings

- GitHub: `Openly-Useful/openlyuseful.org` draft PR #8 and cross-repository
  identity issue `MeekPhills/citewire#3`.
- Linear: `KAR-67`.
- Depends on GitHub `MeekPhills/citewire` draft PR #2 / WS-005.

## Claude tier

Level 3 Sonnet owns the bounded link/diff correction and committed validator
after a Level 1 Fable plan. A separate Opus session reviews public destination
and copy safety. Level 0 controls merge sequence and owner gates.

## Reviewer and integrator

- Independent reviewer: Terra for public copy/destination safety.
- Exact-reference reviewer: Luna for changed paths and URL assertions.
- Integrator: Sol parent orchestrator.

## First safe action

Read the current PR #8 state and run its committed validator in a clean
worktree. Do not update or merge it until WS-005 supplies fresh HTTPS proof.

## Escalation

Escalate to Luis before a public merge, deployment, DNS/domain mutation, any
visible copy rewrite, or redefinition of the canonical Citewire destination.
