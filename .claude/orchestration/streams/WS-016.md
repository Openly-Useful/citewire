# WS-016 — Controlled canary and rollback drill

## objective

Prepare and, only after explicit owner authorization, run a bounded canary for the rights-safe automated editorial workflow. Lower score bands and Adjacent signal remain shadow-only until policy, console, assessments, and rollback are proven.

## status

PLANNED — no canary, deployment, threshold change, or automated publishing is authorized.

## provenance

- Production baseline: main at 9c1506320771faf177f3675ef5ea1c1eff36fe81.
- Required order in Karaya issue 19/KAR-70: production truth; policy/data contract; deterministic replay/shadow; assessments; read-aloud approval; exceptions console/global pause; canary/rollback.
- Existing production gate, review fallback, revisions, audit trail, and smoke tests are the rollback baseline.

## last-good

Current production remains healthy and unchanged. No new automated workflow is enabled by this program.

## completed

Required order and owner gates are documented. Existing tests and production verification provide a rollback baseline. No deployment artifact exists.

## remaining

- Write a runbook for cohort/source scope, feature flag, holds, pause, metrics, abort criteria, and rollback.
- Prove replay, pause, and rollback using committed tests in a non-production shadow context.
- Obtain Luis read-aloud approval and separate canary deployment approval.
- If approved, record exact commit, deployment, cohort, times, assessments, and rollback evidence.

## unknowns

Canary cohort/source, deployment target, flag owner, monitoring, abort thresholds, retention, support, and cost are unresolved. No provider/model is selected.

## dependencies

WS-012 truth; WS-014 shadow policy; WS-015 exceptions/pause; explicit owner
deployment approval. WS-013 generic contract is an optional consult, not a hard
dependency.

## owned paths

In the required fresh `karayagroup-production-audit` worktree only:
`site/api/cron/ingest.js`, `site/vercel.json`,
`docs/industry-news-runbook.md`, `docs/go-live-runbook.md`, and
`docs/autonomous-editorial-canary-runbook.md`. Earlier policy, console, and test
paths are read-only unless Level 0 records an explicit serialized ownership
transfer after predecessor completion.

## binary DoD

- [ ] Reviewed runbook defines bounded cohort, kill switch, hold, metrics, abort criteria, rollback, and evidence.
- [ ] Shadow/replay and pause/rollback tests pass.
- [ ] Luis approves visible copy and separately authorizes canary deployment.
- [ ] If executed, canary is auditable and promoted/rolled back only by explicit owner decision.
- [x] No canary, deployment, threshold/source/provider activation, migration, or automated publishing occurred.

## artifact

Planned `docs/autonomous-editorial-canary-runbook.md`, committed pause/rollback regressions, and only if approved a dated canary evidence record. No live deployment artifact exists.

## baseline

Production 9c150632... and current test/build baseline; exclude all 49 owner screenshots from the stream, including 41 untracked and 8 tracked files.

## focused checks

Run existing policy, ingest, admin, revision, and MCP tests; full build/registry/audit/compliance/diff/secret checks for runtime changes; deterministic pause/abort/replay/rollback checks.

## integration checks

Confirm no publication on rights/attribution failure or disagreement, global pause blocks every path, and production/link state remains unchanged without approval.

## rollback

Capture the pre-canary SHA and deployment. On abort, pause first, stop publishing, restore prior configuration/commit, and verify public smoke and audit evidence. Never delete history or force-reset a shared branch.

## GitHub mapping

Issue 19: https://github.com/MeekPhills/karayagroup/issues/19. No canary PR or deployment exists.

## Linear mapping

Parent KAR-66; work item KAR-70; related KAR-53.

## Claude tier

Level 1 nested Fable for sequencing, Level 2 Opus rollout review, Level 3 Sonnet for bounded tests only.

## reviewer

Terra rollout/rollback review; Luna exact deployment/commit and screenshot-scope review.

## integrator

Level 0 Sol/Fable master is the only execution coordinator. Luis retains deployment/copy gates.

## first action

Confirm WS-012 through WS-015 are complete and independently reviewed. If not, keep this stream blocked.

## escalation

Stop on failed pre-canary test, secret, migration, activation, paid-service/model decision, Vercel/DNS action, unapproved copy, or irreversible action without owner approval.
