# WS-008 — Immutable Community release and public registry gate

## Objective

Run the immutable-release sequence for a final approved Community candidate
only after all technical, identity, rights, and owner gates are satisfied.

## Status

**Not started / manual owner gate.** No release environment, tag, GitHub
release, npm `citewire@0.2.0`, MCP Registry entry, or catalog submission is
verified as complete.

## Provenance

- Community candidate `10aee95ac95bb437b4607723cd20fa5c8f9d1879`, draft PR #1
- `docs/releasing.md` and `docs/distribution.md`
- Historical release review in `CITEWIRE_CODEX_HANDOFF.md` and
  `CITEWIRE_EXECUTION_CHECKPOINT.md`

Trust: high for the unperformed release status and documented gates; current
organization/npm/registry configuration is Unknown until freshly inspected.

## Last-good state

The package is not published beyond npm `citewire@0.1.0`. Community remains a
draft, zero-dependency, provider-disabled candidate. Cloud uses the immutable
Community archive pin `10aee95` but remains draft and undeployed.

## Completed

- Release workflow protection and preflight requirements are documented.
- Historical Community release checks, provider condition repairs, secret scan,
  MCP manifest validation, and draft CI gates passed.
- No irreversible release action was taken during recovery.

## Remaining

- Fresh final code/release review and exact main-ancestor confirmation.
- Owner configures or approves protected GitHub `release` environment with Luis
  as required reviewer and `v*` tag restriction.
- Owner verifies private vulnerability reporting and npm trusted publishing for
  the exact repository, workflow, and environment.
- Owner authorizes merge, no-publication release preflight, then each tag,
  publication, GitHub release, registry, and catalog action separately.
- Independently verify every public record before updating distribution status.

## Contradictions and unknowns

- Draft CI success and a passing no-publication preflight do not authorize a
  tag or publication.
- `citewire@0.2.0` availability, release environment, private vulnerability
  reporting, npm publisher trust, Registry record, and catalog state are
  Unknown until fresh provider-facing read checks.
- WS-002 may change the canonical identity and must resolve before a permanent
  release identity is selected.

## Dependencies

- WS-001 orchestration control.
- WS-002 owner-approved canonical identity.
- WS-004 final Community verification.
- Explicit Luis approval for every irreversible operation.

## Owned paths

Community release workflow, package/version metadata, release documentation,
and distribution records only after a separately approved scoped release task.
No Cloud/Karaya/runtime/provider/DNS path is owned here.

## Binary definition of done

- Fresh tests and release checks pass for the exact approved main commit.
- Protected environment and npm trusted-publishing configuration are verified
  by the owner and recorded without exposing credentials.
- A manual no-publication preflight succeeds with both publication toggles
  false.
- Luis separately authorizes tag, npm publication, GitHub release, MCP Registry
  publication, and catalog submission; each completed record is independently
  visible and accurate.
- `docs/distribution.md` remains `PENDING` until the corresponding public
  record is observed.

## Artifact

Exact release decision record, preflight logs without secrets, immutable
commit/tag mapping, and independent public-record verification evidence.

## Checks

- **Baseline:** isolated-cache install; `npm test`; `npm run release:check`;
  package/diff/secret checks; current main ancestry and clean-worktree check.
- **Focused:** reuse committed provider, release-metadata, workflow, manifest,
  and packaging regressions; inspect OIDC/workflow/environment configuration
  without reading credentials.
- **Integration:** Terra independently reviews rights, release sequencing,
  public claims, and ownership gates; Luna reconciles exact version/tag/URL
  identifiers; Sol confirms no downstream Cloud/website assertion outruns the
  immutable public record.

## Rollback

Before publication, do not create an immutable record. After any owner-approved
immutable action, use the documented incident/release response rather than
destructive tag deletion or unpublish. Correct public records with a new,
explicitly authorized release if required.

## Mappings

- GitHub: `MeekPhills/citewire` draft PR #1, identity issue #3, and future
  separately approved release.
- npm: `citewire@0.1.0` is historical current public baseline; `0.2.0` is not
  yet verified published.
- Registry/catalog: no verified public entry.
- Linear: `KAR-67`; historical release preparation `KAR-48`.

## Claude tier

Level 0 Fable owns immutable-gate orchestration. Level 1 Fable plans only an
approved release sequence. Level 2 Opus may run a scoped preflight. Level 3
Sonnet validates committed tests and metadata. No tier may publish without Luis.

## Reviewer and integrator

- Independent reviewer: Terra release and rights reviewer.
- Exact-reference reviewer: Luna for package/tag/registry identifiers.
- Integrator: Sol parent orchestrator.

## First safe action

Freshly inspect the current PR #1, release environment, npm trusted-publisher,
package version, GitHub tags/releases, and registry/catalog visibility read
only. Record Unknown rather than attempting configuration.

## Escalation

Escalate to Luis before protected-environment creation/configuration, privacy
or vulnerability-reporting setting change, npm trust configuration, merge,
tag, package publication, GitHub release, Registry/catalog submission, provider
activation, paid-service decision, or any irreversible public record.
