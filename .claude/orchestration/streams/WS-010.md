# WS-010 — Post-publication Cloud npm-range migration

## objective

Replace Cloud's temporary immutable Community archive pin with the verified
published npm range only after WS-008 completes and public provenance is
independently verified.

## status

BLOCKED — `citewire@0.2.0` is not published and no dependency change is
authorized.

## provenance

- Cloud head `69068275ca0e332e36304533aa0f6b5d2e291405` retains the exact
  `10aee95ac95bb437b4607723cd20fa5c8f9d1879` archive pin.
- The archive-pinned clean install, 15/15 committed tests, direct HTTP smoke,
  concurrent account-isolation tests, syntax/diff checks, and Node 18/22 CI are
  verified predecessor evidence recorded by WS-009.
- The documented future target is `^0.2.0`, but no npm artifact exists yet.

## remaining

- Complete WS-008 under separate owner approvals.
- Independently verify the published package's version, `mcpName`, integrity,
  provenance, and canonical repository identity.
- Change only `package.json` and `package-lock.json`, then reproduce the full
  committed Cloud verification suite. Keep the PR draft and undeployed.

## dependencies

- WS-008 immutable Community release and public provenance verification.
- WS-009 provides the completed archive-pin baseline; WS-011 does not depend on
  this later migration.

## owned paths

- `/Users/luismorrobel/karayagroup/citewire-cloud/package.json`
- `/Users/luismorrobel/karayagroup/citewire-cloud/package-lock.json`

## binary DoD

- [ ] npm independently reports the exact owner-approved Community artifact.
- [ ] Both Cloud manifests use the documented npm range and expected integrity.
- [ ] Isolated-cache clean install, full committed suite, syntax/diff checks,
  direct HTTP smoke, and concurrent account-isolation tests pass.
- [ ] Node 18/22 CI is green on the exact draft PR head.
- [ ] No deployment, merge, tag, publication, registry action, provider
  activation, secret, paid-service decision, or account-boundary regression
  occurs.

## checks

Use `/private/tmp/citewire-cloud-npm-cache`, the repository's committed tests,
and its documented direct HTTP and concurrent account-isolation smokes. Do not
create ad hoc test artifacts.

## artifact

The expected output is the focused, independently reviewed two-manifest npm
range change in draft Cloud PR #1 plus recorded clean-install, test, direct HTTP,
concurrent account-isolation, diff, and Node 18/22 CI evidence.

## rollback

Revert the focused two-manifest dependency change before merge and retain the
verified exact archive pin. Never substitute an unverified range or copied
transport.

## mappings

- GitHub: draft `MeekPhills/citewire-cloud` PR #1.
- Linear: KAR-66, with Community release authority tracked in KAR-67.

## Claude hierarchy

Opus owns dependency provenance and account-boundary interpretation. A separate
Terra session independently reviews supply-chain and account-isolation evidence.
Level 0 integrates.

## first action

After WS-008 only, read public npm provenance for the exact released Community
version. Leave both Cloud manifests untouched until that evidence passes.

## escalation

Stop on absent or wrong provenance, a failed test, unexpected lockfile diff,
secret, npm publication request, deployment, merge, or account crossover.
