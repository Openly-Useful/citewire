# WS-015 — Adjacent signals, exceptions console, and assessments

## objective

After WS-012 and WS-014 prove the automated path, replace routine review with an exceptions-only surface. Expose controlled reason tags, Adjacent signal handling, decision traces, health and assessments, explicit reprocess/reject/one-item approval, and a tested global pause.

## status

PLANNED — current console remains live and unchanged; no adjacent UI or automated publishing exists.

## provenance

- Karaya production baseline: main at 9c1506320771faf177f3675ef5ea1c1eff36fe81.
- Existing console: /admin and read-only /admin/sources; remaining dedupe/dead-letter/kill-switch/source-activation work is anchored by KAR-53.
- No Adjacent signal UI, inclusion-tier fields, new agent workflow state, durable trace, or assessment scheduler exists.
- Cadence from KAR-66: 15-minute ingest, hourly health, daily audit, weekly calibration, monthly source/provider/security/cost review.

## last-good

Existing authorization, publication gate, revisions, audit rows, and source-health view remain the baseline. Routine review is not removed yet.

## completed

Current admin routes, auth, source-health helpers, publication/revision helpers, and committed tests are available. Target exception actions and cadence are requirements, not implementation claims.

## remaining

- Define exception schema and reason tags.
- Show inclusion tier, source attribution, agent agreement, trace, and health without secrets/article bodies.
- Implement and test reprocess, reject, explicit approval, and global pause as audited/replay-safe actions.
- Add assessment outputs and failed-agent escalation.
- Obtain read-aloud approval for badge and reason-tag copy.

## unknowns

Final roles, trace retention, assessment storage/scheduler, alert destination, exception SLA, exact copy, and paid-service need are unresolved.

## dependencies

WS-012 and WS-014 are hard prerequisites. WS-013 supplies generic fields only. WS-016 waits for pause and rollback tests.

## owned paths

In the required fresh `karayagroup-production-audit` worktree only:
`site/api/admin.js`, `site/api/_lib/editorial-auth.js`,
`site/api/_lib/editorial-queries.js`, `site/api/news-page.js`,
`site/api/_lib/render.js`, `site/src/pages/industry-news/shell.astro`,
`site/src/styles/`, `site/test/admin.test.mjs`, `site/test/news-page.test.mjs`,
and `docs/adjacent-signal-console-runbook.md`. The PR #17-owned
`about.astro` and `news-compliance.test.mjs` are explicitly excluded. Shared
paths require predecessor completion and a recorded Level 0 ownership transfer.

## binary DoD

- [ ] Routine bypass only follows deterministic rights, attribution, quality, and independent-agreement gates.
- [ ] Exceptions UI shows tier, reason, attribution, agreement, trace, and health.
- [ ] Reprocess, reject, approval, and global pause are authorized, audited, replay-safe, and tested.
- [ ] Assessment cadence and escalation are reproducible.
- [ ] Visible copy has Luis read-aloud approval.
- [ ] Existing schema is sufficient, or a separately approved reversible
  migration/backfill/configuration subtask with snapshot and rollback evidence
  has completed before implementation resumes.
- [ ] No production publishing expansion or provider/model/cost choice.

## artifact

Planned `docs/adjacent-signal-console-runbook.md`, committed route/policy tests, and assessment fixtures. No ad hoc artifacts or live queue rows.

## baseline

Production 9c150632... and current console/auth baseline; preserve owner screenshots.

## focused checks

Run existing admin auth, publication, revision, source-health, and MCP tests; full build/registry/audit/compliance/diff/secret checks for runtime changes; deterministic pause/replay tests.

## integration checks

Confirm pause blocks automated and manual publishing and writes a truthful audit result. Confirm role boundaries, attribution, revisions, source terms, and no Community leakage.

## rollback

Keep current review/publication gates. If pause or audit fails, disable only the new workflow and hold items for the existing path. Do not delete history.

## GitHub mapping

Issue 19: https://github.com/MeekPhills/karayagroup/issues/19. Related follow-up: KAR-53.

## Linear mapping

Parent KAR-66; work item KAR-70; related KAR-53.

## Claude tier

Level 2 Opus for authorization/audit/failure modes; Level 1 nested Fable coordination; Level 3 Sonnet for bounded UI/tests after approval.

## reviewer

Terra security/rights/state-transition review; Luna route/test/screenshot-scope review.

## integrator

Level 0 Sol/Fable master owns synchronization.

## first action

Inventory mutating admin paths and current tests before designing the exceptions-only surface.

## escalation

Stop on public copy without approval, auth/RLS change, migration, queue mutation, source toggle, provider/model/cost choice, secret, deployment, or failed pause test.
