# WS-001 — Orchestration evidence and execution control

## Objective

Keep a single, evidence-backed execution record for the Citewire open-source
program. This stream coordinates work. It does not implement product behavior,
create accounts, or make public changes.

## Status

**Partial / safe to continue as documentation only.** Recovery artifacts are
being established on `agent/citewire-orchestration-handoff`; no execution
authority is created by them.

## Provenance

- `CITEWIRE_CODEX_HANDOFF.md` and `CITEWIRE_EXECUTION_CHECKPOINT.md`
- Current local Citewire worktree at
  `ff09b68ed811531eb0ca630b65daeaf06cc3304c`
- `.claude/orchestration/recovery-ledger.yaml`

Trust: high for local Git and current artifacts; high but historical for the
dated checkpoint and handoff.

## Last-good state

The program had no durable `.claude/orchestration/` package. Community, Cloud,
landing, Openly Useful, and Karaya work were separated into draft branches and
owner-gated follow-on actions.

## Completed

- Pickup evidence was consolidated without changing product code.
- The recovery ledger records exact SHAs, PRs, incomplete owner gates, and
  protected owner assets.

## Remaining

- Keep recovery, work graph, execution state, verification evidence, handoff,
  and stream briefs mutually consistent.
- Re-read remote state before any GitHub, Linear, or deployment mutation.
- Record outcomes only after the described checks have actually passed.

## Contradictions and unknowns

- Root Karaya deployment documentation is stale and cannot govern the live
  Industry News program.
- `CITEWIRE_CLAUDE_CODE_HANDOFF.md` is missing; do not claim it was recovered.
- Remote PR, CI, Vercel, GitHub, and Linear states require fresh checks.

## Dependencies

None. This stream is the prerequisite control record for WS-002 through
WS-008.

## Owned paths

- `PROJECT_HANDOFF.md`
- `CLAUDE.md` managed orchestration block only
- `.claude/orchestration/**`

No product, configuration, screenshot, or deployment path is owned here.

## Binary definition of done

- Every active stream has a concise, independently executable brief.
- Recovery facts distinguish Verified, Historical, Inferred, Unknown, and
  Contradicted claims.
- Every planned mutation has a named owner, check, rollback, and approval
  boundary.
- YAML parses and all intra-artifact stream IDs and paths resolve.

## Artifact

`PROJECT_HANDOFF.md`, `.claude/orchestration/recovery-ledger.yaml`,
`.claude/orchestration/work-graph.yaml`,
`.claude/orchestration/execution-state.yaml`,
`.claude/orchestration/verification-evidence.md`, and this stream directory.

## Checks

- **Baseline:** parse all orchestration YAML and inspect `git diff --check`.
- **Focused:** verify every cited SHA, PR, path, and owner gate against fresh
  local or remote evidence before changing its claim status.
- **Integration:** independently review the handoff and work graph for missing
  dependencies, duplicate owners, accidental production steps, and forbidden
  screenshot paths.

## Rollback

Revert only the focused orchestration commit or restore the last verified
artifact version. Never use broad reset/clean operations and never touch owner
screenshots.

## Mappings

- Linear: `KAR-66` (open-source program); relates to completed `KAR-48` and
  `KAR-36`.
- GitHub: coordination only; no PR is merged by this stream.

## Claude tier

Level 0 Fable master coordinator. Delegate bounded evidence or implementation
work only after this record identifies a safe owner and dependency order.

## Reviewer and integrator

- Independent reviewer: Terra for work-graph and contradiction review.
- Exact-reference reviewer: Luna.
- Integrator: Sol parent orchestrator.

## First safe action

Parse the orchestration YAML, run `git diff --check`, and reconcile only
evidence that can be inspected read-only.

## Escalation

Stop for Luis if a stream would create a repository, alter identity or legacy
history, merge, deploy, change DNS, activate a provider, migrate data, choose
a paid service, publish, tag, release, submit a registry/catalog, or change
visible Karaya copy.
