# WS-004 — Community candidate and provider-compliance release gate

## Objective

Preserve and independently re-verify the Community candidate at
`10aee95ac95bb437b4607723cd20fa5c8f9d1879` while keeping the immutable release
path owner-gated.

## Status

**Code complete / immutable release blocked.** `MeekPhills/citewire` draft PR
#1 remains the release candidate. It is not merged, tagged, published, or
submitted to any registry/catalog.

## Provenance

- Community branch `agent/citewire-product-launch`
- SHA `10aee95ac95bb437b4607723cd20fa5c8f9d1879`
- Draft PR #1: `https://github.com/MeekPhills/citewire/pull/1`
- Historical checks: 66/66 tests, 5/5 release checks, 22 syntax checks,
  manifest/package/diff/secret gates, Node 18 and Node 22 CI green

Trust: high historical evidence, requiring a fresh check immediately before a
merge or release action.

## Last-good state

The candidate is MIT, dependency-free, Node 18+ ESM, provider-disabled by
default, and has documented attribution, pacing, bounded excerpts, and
operator responsibilities. npm contains `citewire@0.1.0`; `0.2.0` is not
published.

## Completed

- Provider-condition and release-hardening work is in the current candidate.
- GDELT/Semantic Scholar attribution, arXiv/dblp pacing, Crossref contact
  configuration, Europe PMC acknowledgment, canonical links, and current
  OpenAlex documentation are recorded as repaired.
- The official MCP publisher validation historically passed.

## Remaining

- Fresh read-only PR, CI, provider-condition, and release-workflow review.
- Owner setup for protected `release` environment, private vulnerability
  reporting, npm trusted publishing, and final review.
- Separate owner approval for merge, preflight, tag, npm publication, GitHub
  release, MCP Registry publication, and catalog submission.

## Contradictions and unknowns

- Old references to Community pin `6c89b6e` are superseded by `10aee95`.
- A green draft PR does not create release authority.
- Actual Crossref contact, shared scaling limiter, current upstream terms, and
  all public registry visibility are operationally Unknown until checked.

## Dependencies

- WS-001 evidence control.
- WS-002 identity decision for any canonical-repo change.
- WS-008 is the onward immutable-release gate and depends on this completed
  candidate verification; it is not a prerequisite for WS-004.

## Owned paths

`src/`, `test/`, `docs/providers.md`, `docs/releasing.md`,
`docs/distribution.md`, package metadata, and release workflow only when a
separately scoped Community change is approved.

## Binary definition of done

- Fresh baseline and focused Community checks pass on the exact SHA.
- The candidate remains dependency-free, ESM, provider-disabled by default,
  and attribution/rights/pacing checks remain covered by committed regressions.
- Required release setup has fresh evidence and Luis separately authorizes each
  immutable step.
- Public records are independently visible before `docs/distribution.md` stops
  saying `PENDING`.

## Artifact

Fresh Community verification evidence and, only after approval, a focused PR or
release-preflight record tied to the exact commit.

## Checks

- **Baseline:** isolated-cache clean install; `npm test`; `npm run
  release:check`; documented syntax, package, diff, and secret checks.
- **Focused:** reuse committed provider, HTTP, RPC, pacing, platform, and
  release-metadata regressions; validate MCP manifest and release workflow
  pinning without ad hoc artifacts.
- **Integration:** Terra independently reviews provider compliance, release
  preconditions, rights/attribution posture, and the downstream Cloud archive
  pin; Sol reconciles the result with WS-005 and WS-008.

## Rollback

Before a release, remain on the verified candidate and make no immutable
change. For an approved focused code fix, revert only the focused commit after
tests. Never delete tags, unpublish packages, or alter registry history as a
routine rollback.

## Mappings

- GitHub: `MeekPhills/citewire` draft PR #1 and identity issue #3.
- Linear: `KAR-67`; completed `KAR-48` remains historical provenance.

## Claude tier

Level 2 Opus implementation owner for a scoped Community fix, nested under a
Level 1 Fable plan. Level 3 Sonnet runs focused regressions and documentation
checks. Level 0 Fable retains release gates.

## Reviewer and integrator

- Independent reviewer: Terra release/rights review.
- Exact-reference reviewer: Luna.
- Integrator: Sol parent orchestrator.

## First safe action

Run a fresh read-only review of PR #1 and its exact SHA, then reproduce the
committed baseline checks in an isolated cache. Stop on any failed check or
unexpected diff.

## Escalation

Escalate to Luis before merge, protected-environment configuration, private
vulnerability reporting configuration, npm trusted-publisher configuration,
tag, release, publication, MCP Registry/catalog submission, provider
activation, or paid-service decision.
