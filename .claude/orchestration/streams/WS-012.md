# WS-012 — Karaya production truth and corpus policy integrity

## objective

Establish dated, read-only truth for Karaya Industry News before autonomous editorial expansion. Reconcile live HTTP/MCP/feed observations with ingest and publication policy, classify stored-corpus drift, and prepare owner-reviewed remediation without database writes, source changes, or silent rewrites.

## status

RESEARCH READY — live surface is healthy; corpus and operational state need a fresh evidence artifact.

## provenance

- Production reference: repaired Karaya main at 9c1506320771faf177f3675ef5ea1c1eff36fe81.
- Required code worktree: `/Users/luismorrobel/karayagroup/karayagroup-production-audit`, freshly created as a clean isolated checkout from verified `origin/main` and recorded by Level 0 before this stream begins.
- Never use `karayagroup-live` for production truth and never use or write the protected `karayagroup-brand` owner-screenshot worktree.
- On 2026-08-15, /, /industry-news, /industry-news/about, and the topics API returned 200.
- Topics returned nine slugs: ai-compute, data-centers, power-cooling, gpu-systems, cloud-colocation, capital-financing-ma, supply-chain, policy-governance, and benchmarks-research.
- MCP initialize at /mcp succeeded with protocol 2025-06-18, server karaya-news-mcp version 1.0.0, and read-only instructions.
- The observed 50-item production JSON feed contained 16 summaries over the provisional 480-character cap and one appeared first on POWER Magazine trailer.

## last-good

Current code strips known boilerplate and applies the 480-character gate. Live oversize/trailer observations establish stored-corpus drift, not proof of an ingest-gate bypass. Production is intentionally unchanged.

## completed

- Public Industry News, About, topics API, and MCP surfaces are reachable.
- Current publication policy, source conditions, revisions path, and summary cap are documented.
- GitHub issue #18 and Linear KAR-69 capture this scope.
- Owner screenshots remain untouched: 49 files exist in the owner handoff area, including 41 untracked assets in the separate owner worktree.

## remaining

- Record read-only migration 0007–0010 application state.
- Record effective source/circuit-breaker state, cron health, queue age, latest runs, and current production source count.
- Classify each affected row by policy version, source mode, length, boilerplate, attribution, rights posture, and revision history.
- Present remediation options that preserve revisions and audit history. Any write, migration, source toggle, provider activation, or rewrite is separate.

## unknowns

Live migration state, effective source count versus registry count, circuit-breakers, cron/queue health, affected-row provenance, and revision lineage are not established by public HTTP smoke alone.

## dependencies

- No implementation dependency for read-only truth collection.
- WS-014 waits for this evidence; WS-015 and WS-016 depend on the classified baseline.

## owned paths

- Read-only inspection of runtime and migration paths in the fresh production-audit worktree.
- Sole write ownership: `/Users/luismorrobel/karayagroup/karayagroup-production-audit/docs/production-truth-2026-08-15.md`.
- No runtime, migration, test, configuration, database, corpus, or protected-owner-asset write ownership.

## binary DoD

- [ ] Dated read-only evidence covers migrations, effective sources, circuit-breakers, cron, queue, runs, and source count.
- [ ] Every affected row has policy/source/length/boilerplate/attribution/rights/revision classification.
- [ ] Code, registry, database, and public-feed contradictions are resolved or marked Unknown.
- [ ] Remediation preserves audit/revision history and remains owner-gated.
- [x] No database write, migration, source toggle, provider activation, or silent rewrite occurred.
- [x] Owner screenshots remain unmodified, unstaged, and untracked.

## artifact

Planned `docs/production-truth-2026-08-15.md` with dated evidence, row-level classification, and no-write disposition. Existing committed tests are inputs; do not create ad hoc test artifacts.

## baseline

Live production `9c150632...`; the implementation baseline must be refreshed from verified `origin/main`. The historical `karayagroup-live` and protected `karayagroup-brand` worktrees are excluded.

## focused checks

Routine HTTP smoke for public pages/endpoints; read-only MCP initialize and tool inspection; read-only feed retrieval with deterministic count/length/trailer report; existing build/test/registry/audit/compliance checks only as needed. Never apply migrations or invoke mutating admin APIs.

## integration checks

- Compare live 480-character observations with publish-policy.js and ingest normalization.
- Compare live source/circuit state with registry without treating YAML count as runtime truth.
- Pass the classified baseline to WS-014.

## rollback

Evidence is read-only. If proof would require mutation, record Unknown and stop. Delete no rows, rollback no migrations, and rewrite no stored content.

## GitHub mapping

- Issue #18: https://github.com/MeekPhills/karayagroup/issues/18.
- Link PR #17 is separate and remains draft.

## Linear mapping

- Parent: KAR-66.
- Work item: KAR-69.
- Existing console/source-health follow-up: KAR-53.

## Claude tier

Level 2 Opus for production-readiness and contradiction analysis; Level 3 Sonnet for bounded read-only endpoint evidence under review.

## reviewer

Terra reconciles code, feed, and operational-state claims. Luna checks URLs, SHAs, migration names, counts, and screenshot preservation.

## integrator

Level 0 Sol/Fable master controls the dependency gate into WS-014.

## first action

Level 0 first records the fresh clean production-audit worktree and exact base SHA. Then run routine public smoke tests and MCP initialize before any privileged read-only inspection.

## escalation

Stop on database write, migration, source toggle, provider activation, secret, paid-service decision, queue mutation, silent rewrite, screenshot change, or contradiction that cannot be marked Unknown.
