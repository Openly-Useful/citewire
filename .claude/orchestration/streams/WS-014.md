# WS-014 — Rights-aware policy, evaluation, and shadow workflow

## objective

Define and validate a deterministic, auditable shadow state machine for Karaya ingest, relevance, summarization, verification, and publishing. Routine human review may be removed only after rights, attribution, evidence, and independent-agent agreement gates are proven.

## status

PLANNED — shadow/replay only; production thresholds remain unchanged.

## provenance

- Karaya production baseline: main at 9c1506320771faf177f3675ef5ea1c1eff36fe81.
- Current bands: below 0.55 quarantine; 0.55–0.8199 review; at least 0.82 auto-eligible subject to the full gate.
- Proposed shadow bands: below 0.50 held; 0.50–0.5999 adjacent-only after independent agreement with an Adjacent signal badge/reason tag; at least 0.60 standard candidate.
- Existing policy, attribution/redaction gates, source conditions, revisions, and tests are the baseline.

## last-good

Unknown rights, missing attribution, provider failure, agent disagreement, weak evidence, and policy conflicts hold an item. No live threshold or publication behavior changed.

## completed

- Existing policy gate, summary cap, source-mode rules, revisions, and audit behavior are implemented and tested.
- Automated review/summarization/posting and recurring assessment direction is recorded without a model/provider/cost choice.
- GitHub issue 19 and Linear KAR-70 capture required order and holds.

## remaining

- Version the policy/data contract and immutable decision-trace fields.
- Replay the classified corpus after WS-012; shadow output must not expand publication.
- Define independent roles/agreement and controlled reason tags.
- Define 15-minute ingest, hourly health, daily audit, weekly calibration, and monthly source/provider/security/cost assessments.
- Carry only a non-visible label token or proposed copy. WS-015 exclusively owns
  the read-aloud gate for any visible label or reason-tag copy.

## unknowns

Historical row policy versions, rights lineage, agreement threshold, calibration quality, summary method, provider/model terms, cost ceiling, and badge copy are unknown.

## dependencies

WS-012 is a hard prerequisite. WS-013 may supply only generic fields. WS-015 and WS-016 wait for this contract.

## owned paths

In the required fresh `karayagroup-production-audit` worktree only:
`docs/adr-editorial-policy-shadow-state-machine.md`,
`site/api/_lib/ingest/classify.js`, `site/api/_lib/ingest/process.js`,
`site/api/_lib/publish-policy.js`, `site/test/ingest.test.mjs`,
`site/test/ingest-worker.test.mjs`, and `site/test/publish-policy.test.mjs`.
No `karayagroup-live` or `karayagroup-brand` path is writable.

## binary DoD

- [ ] Versioned state machine names every gate and transition.
- [ ] Replay and independent agreement are deterministic and hold unknown/disagreeing cases.
- [ ] Lower bands remain shadow-only and live thresholds unchanged.
- [ ] Assessment outputs are reproducible.
- [ ] Any label is non-visible placeholder data only; visible-copy approval is
  deferred to WS-015.
- [ ] WS-012 proves existing schema sufficiency. If a migration, backfill, or
  configuration write is needed, this stream stops for a separately approved,
  reversible subtask with snapshot and rollback evidence.
- [x] No provider/model/cost choice, migration, activation, threshold change, or automated production publish.

## artifact

Planned `docs/adr-editorial-policy-shadow-state-machine.md`, deterministic replay fixtures/results, and updates to existing committed policy tests. No ad hoc fixtures.

## baseline

Karaya production 9c150632... and current policy tests; preserve all 49 owner screenshots, including 41 untracked and 8 tracked files.

## focused checks

Run existing policy/ingest tests and full Karaya build/test/registry/audit/compliance gates if runtime files change. Use committed deterministic replay data.

## integration checks

Compare replay with WS-012 classification; keep traces private to Karaya; confirm production thresholds and publication counts are unchanged.

## rollback

Retain the report, keep affected items held/reviewed under current rules, and revert focused implementation before any canary.

## GitHub mapping

Issue 19: https://github.com/MeekPhills/karayagroup/issues/19.

## Linear mapping

Parent KAR-66; work item KAR-70.

## Claude tier

Level 2 Opus for policy/rights/evaluation; Level 1 nested Fable for role coordination; Level 3 Sonnet for bounded tests.

## reviewer

Terra rights/safety reviewer; Luna deterministic fixture and path reviewer.

## integrator

Level 0 Sol/Fable master enforces sequence and approvals.

## first action

Read WS-012 evidence and existing policy tests; inventory states before touching a threshold or UI string.

## escalation

Stop on live threshold/publishing change, activation, model/provider/cost choice, migration, secret, rights uncertainty, or unapproved visible copy.
