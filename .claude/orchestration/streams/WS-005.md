# WS-005 — Citewire landing and HTTPS destination gate

## Objective

Bring the already-tested Citewire landing artifact to a verified HTTPS
destination only after Luis approves the exact Vercel project, domain, and DNS
operations. Keep all public-link consumers on safe destinations until then.

## Status

**Implementation complete / public deployment blocked.** The local landing is
draft PR #2. The live `citewire.org` destination is not usable for the new
site.

## Provenance

- Branch `agent/citewire-landing-site`
- SHA `ff09b68ed811531eb0ca630b65daeaf06cc3304c`, based on Community
  `10aee95ac95bb437b4607723cd20fa5c8f9d1879`
- Draft PR #2: `https://github.com/MeekPhills/citewire/pull/2`
- Historical Node 18/22 runs: `31849200115`, `31849291099`
- Live 2026-08-14/15 evidence: `citewire.org` A records
  `207.207.210.107` and `207.207.210.229`; HTTP parked/default host; HTTPS
  handshake failure; LGAM Vercel team has no Citewire project

Trust: high for local branch and recorded live observation; repeat live checks
before any public mutation.

## Last-good state

The dependency-free static `site/` implementation has canonical/social
metadata, self-hosted OFL fonts, an Openly Useful-inspired visual system,
locked-down Vercel configuration, and committed regressions. It is not the live
domain. Karaya currently links to working GitHub Community instead.

## Completed

- Exact 20-file landing and website-metadata PR is drafted and clean.
- Historical isolated-cache, Node 18/22, release/syntax, secret/diff, nine-path
  routine HTTP smoke, and browser-width checks passed.
- npm dry run excludes `site/`; Community package surface remains separate.

## Remaining

- Freshly verify PR #2 and its stacked base.
- Obtain explicit approval for Vercel project creation or selection, domain
  attachment, DNS records, and production deployment.
- Deploy only after approval, then independently verify `https://citewire.org`
  delivers the intended landing, TLS is valid, canonical/assets work, and no
  old parked content remains.
- Only then unblock WS-006 and WS-007 link merges.

## Contradictions and unknowns

- Passing local/preview checks are not proof of live-domain readiness.
- The known parked HTTP response and failed HTTPS handshake contradict any
  claim that citewire.org already hosts the landing.
- Hosting project ownership, DNS authority, current registrar configuration,
  desired domain topology, and production deployment authority are Unknown.

## Dependencies

- WS-001 evidence control.
- WS-004 Community candidate and WS-002 identity decisions for final public
  metadata direction.
- Explicit Luis approval for all provider, domain, and DNS mutations.

## Owned paths

`site/**`, website/package metadata, tests covering the site, and a future
approved Vercel configuration. DNS and provider settings are not owned until
the owner approves them.

## Binary definition of done

- PR #2 remains a focused 20-file landing/metadata diff on its verified base.
- Fresh local baseline, focused site regressions, and routine HTTP smoke pass.
- Luis authorizes project/domain/DNS/deployment in writing.
- Independent live HTTPS checks prove correct content, valid TLS, canonical
URL, robots/sitemap/assets, and no parked host; then WS-006/WS-007 may proceed.

## Artifact

Focused landing PR evidence, owner-approved domain/deployment runbook, and
post-deployment live verification record. No provider changes before approval.

## Checks

- **Baseline:** isolated-cache install; Community `npm test`; `npm run
  release:check`; site syntax/diff/secret checks; npm dry-run exclusion.
- **Focused:** reuse `test/site.test.mjs` and the committed metadata, link,
  accessibility/default, and hosting-policy regressions; run the routine local
  HTTP and browser-width smoke suite.
- **Integration:** independent Terra review of HTTPS, public claims, and
  cross-site link safety; Luna verifies exact URLs and delivery headers; Sol
  confirms WS-006/WS-007 stay blocked until proof exists.

## Rollback

Before deployment, rollback is no provider or DNS change. After an approved
deployment, preserve the prior DNS/hosting configuration and use an
owner-approved reversible route change. Do not delete records, projects, or
release history as an automatic rollback.

## Mappings

- GitHub: `MeekPhills/citewire` draft PR #2 and identity issue #3, stacked on
  draft PR #1.
- Linear: `KAR-67`.
- Downstream dependencies: Openly Useful PR #8 and Karaya PR #17.

## Claude tier

Level 1 Fable plans public deployment gates. Level 2 Opus owns a scoped,
approved landing/hosting change. Level 3 Sonnet runs static regressions and
routine smoke checks. Level 0 Fable retains public-mutation authorization.

## Reviewer and integrator

- Independent reviewer: Terra for design, public-claim, and deployment review.
- Exact-reference reviewer: Luna for DNS/URL/header evidence.
- Integrator: Sol parent orchestrator.

## First safe action

Repeat read-only HTTP, HTTPS/TLS, DNS, Vercel-project, and PR checks. Record
the exact result before asking Luis for the separate provider/domain approval.

## Escalation

Escalate to Luis before Vercel project creation, provider activation, domain
attachment, DNS change, production deployment, merge, publication, or any
public claim that citewire.org is live.
